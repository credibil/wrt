name: vault

services:
  vault:
    extends:
      file: ../docker/resources.yaml
      service: wasm32
    build:
      args:
        FEATURES: http,otel,vault,azure
    depends_on:
      - otelcol

include:
  - ../docker/opentelemetry.yaml
