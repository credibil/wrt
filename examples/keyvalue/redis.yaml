name: keyvalue

services:
  keyvalue:
    extends:
      file: ../docker/resources.yaml
      service: wasm32
    build:
      args:
        FEATURES: http,otel,keyvalue,redis
    depends_on:
      - redis
      - otelcol

  redis:
    extends:
      file: ../docker/resources.yaml
      service: redis

include:
  - ../docker/opentelemetry.yaml
