name: blobstore

services:
  blobstore:
    extends:
      file: ../../docker/service.yaml
      service: wasm32
    build:
      args:
        FEATURES: http,otel,blobstore,mongodb
    depends_on:
      - mongodb
      - otelcol

include:
  - oci://ghcr.io/credibil/compose-mongodb:latest
  - oci://ghcr.io/credibil/compose-opentelemetry:latest
