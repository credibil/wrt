interface handler {
    use types.{error};

    /// Sends the message to all websocket peers
    send: func(message: string) -> result<_, error>;

    /// Performs a health check of the websocket service
    health-check: func() -> result<string, error>;
}
