package wasi:websockets@0.1.0;

interface types {
    type error = string;

    /// A peer record type representing a connected websocket peer.
    record peer {
        address: string,
        query: string,
    }
}